import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  genreTag: {
    color: 'white',
    backgroundColor: 'rgb(42,125,40)',
    padding: '2px 10px',
    borderRadius: '4px'
  },
}));
